esphome:
  name: tester3
  friendly_name: Tester3

esp32:
  board: esp32dev
  framework:
    type: esp-idf

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "6AyVia4CZrqroYo6vlo2tVyWAXB5EcfNL1QYzfwdyFc="

ota:
  - platform: esphome
    password: "80b4ab1085de5bb8e273501f312df423"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Tester3 Fallback Hotspot"
    password: "Tu3fOyw27zP3"

captive_portal:

web_server:
  port: 80

i2c:
  sda: 4
  scl: 15
  frequency: 700kHz
  scan: true

# font:
#   - file: "fonts/arial.ttf"
#     id: font1
#     size: 8

display:
  - platform: ssd1306_i2c
    model: "SSD1306 128x64"
    address: 0x3C
    reset_pin: 16
    update_interval: 15s
    lambda: |-
      it.line(0, 0, 50, 50);
